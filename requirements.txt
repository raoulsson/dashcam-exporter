staticmap>=0.5.7
Pillow>=10.0
# Optional but recommended: video-based drive-away detection for parking exits.
# Without these the exporter falls back to GPS-speed detection, which is less
# reliable when parked amid passing people / cars. See VideoMotionDetector.
numpy>=1.24
opencv-python-headless>=4.8
# Menu 7 transcription and speaker diarization.
faster-whisper>=1.1
pyannote.audio>=3.3
# Dev: run-tests.sh greps pyflakes for undefined names / redefinitions -- the two
# crash classes a green unit suite sails past. Without it the check silently skips.
pyflakes>=3.0
